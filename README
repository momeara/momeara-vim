Matthew O'Meara's configuration information

To use:

  git clone https://github.com/momeara/momeara-vim.git
  cd momeara-vim
  ln -s $(pwd)/.emacs.d ~/
  ln -s $(pwd)/.vimrc ~/
  ln -s $(pwd)/.vim ~/
